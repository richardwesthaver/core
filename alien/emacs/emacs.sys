;;; sys.sys --- Emacs Module FFI

;;

;;; Commentary:

;; 

;;; Code:
(defsys :emacs
  :require (:std)
  :components ((:pkg "pkg")
               (:grovel "constants"
                :package :emacs)
               (:file "alien"))
  :provide ((:tests)))
