;;; security.sys --- Linux Security FFI bindings

;;

;;; Commentary:

;; 

;;; Code:
(defsys :security
  :require (:std)
  :components 
 ((:pkg "pkg")
  (:grovel "constants"
   :package :security)
  (:file "pam")
  (:file "seccomp"))
  :provide ((:tests)))
