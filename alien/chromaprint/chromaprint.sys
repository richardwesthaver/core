;;; chromaprint.sys --- CHROMAPRINT SYSTEMS

;; CHROMAPRINT for lisp.

;;; Commentary:

;;; Code:
(defsys :chromaprint
 "CHROMAPRINT C FFI"
 :require (:std)
 :components ((:pkg "pkg"))
 :provide ((:tests)))
