
#+title: core
#+author: Richard Westhaver <ellis@rwest.io>
#+description: The Compiler Company Core
The Compiler Company (=CC=) is a software research laboratory
concerned with the future of Mechanical Freedom.

This repository contains a collection of software libraries shared
across all of our products and projects.

- NOTE :: This is *not* a portable library. Starting in version =1.0=
  we will be depending on separately distributed forks of SBCL and
  Rust. See details here.

To bootstrap the core you will need recent versions of [[https://www.gnu.org/software/emacs/][GNU Emacs]],
[[https://www.rust-lang.org/][Rust]], and [[http://www.sbcl.org/][SBCL]].


* Quickstart
#+name: x-save-self
#+begin_src shell :exports both
# bootstrap the core build tool
./x.lisp
#+end_src

#+RESULTS: x-save-self
: saving self to ./x

#+begin_src shell :results output :exports both
./x --help
#+end_src

#+RESULTS:
: x --- core build tool
: x.lisp [CMD]
: CMDS:
: test
: compile
: build
: run
: save
: install

#+begin_src shell :results none :noeval t
# start a repl
./x
#+end_src

#+begin_src shell :results none :noeval t
# test systems
./x test std
#+end_src

#+begin_src shell :results none :noeval t
# save cores
./x save prelude
#+end_src

#+begin_src shell :results none :noeval t
# build binaries
./x build skel
#+end_src

#+begin_src shell :results none :noeval t
# install binaries
./x install skel
#+end_src

** binaries
*** skel
#+begin_src shell :results output :exports both
./x run skel show :name :id :version :author :description :tags
#+end_src

#+RESULTS:
: core
: 33cf-3485-bc1a-9528
: 0.1.0
: Richard Westhaver <ellis@rwest.io>
: The Compiler Company Core
: (core lisp rust emacs c)
*** organ
#+begin_src shell :results output :exports both
./x run organ
#+end_src

#+RESULTS:
: #<ORGAN::ORG-DOCUMENT {100384A4E3}>
:   [standard-object]
: 
: Slots with :INSTANCE allocation:
:   META                           = #<ORGAN::ORG-META-SECTION {100384CF63}>
:   TREE                           = #(#<ORGAN:ORG-HEADING {1003920C43}>)

