#+title: core
#+author: Richard Westhaver <ellis@rwest.io>
#+description: The Compiler Company Core
This repository contains a collection of software libraries shared
across all of our products and projects.

- NOTE :: This is *not* a portable library. Starting in version =1.0=
  we will be depending on separately distributed forks of SBCL and
  Rust. See details here.

To bootstrap the core you will need recent versions of [[https://www.gnu.org/software/emacs/][GNU Emacs]],
[[https://www.rust-lang.org/][Rust]], and [[http://www.sbcl.org/][SBCL]].


* Quickstart
#+name: x-save-self
#+begin_src shell :exports both
# bootstrap the core build tool
./x.lisp
#+end_src

#+RESULTS: x-save-self
: saving self to ./x

#+begin_src shell :results output :exports both
./x --help
#+end_src

#+RESULTS:
: x --- core build tool
: x.lisp [CMD]
: CMDS:
: build
: run
: test
: save
: install

#+begin_src shell :results none :noeval t
# start a repl
./x
#+end_src

#+begin_src shell :results none :noeval t
# test systems
./x test std
#+end_src

#+begin_src shell :results none :noeval t
# save cores
./x save prelude
#+end_src

#+begin_src shell :results none :noeval t
# build binaries
./x build skel
#+end_src

#+begin_src shell :results none :noeval t
# install binaries
./x install skel
#+end_src

** binaries
*** skel
#+begin_src shell :results output :exports both
./x run skel
#+end_src

#+RESULTS:
#+begin_example
#<SK-PROJECT :ID 33cf-3485-bc1a-9528>
  [standard-object]

Slots with :INSTANCE allocation:
  NAME                           = "core"
  PATH                           = #P"/home/ellis/dev/comp/core/skelfile"
  AUTHOR                         = "Richard Westhaver <ellis@rwest.io>"
  VERSION                        = "0.1.0"
  TAGS                           = ("core")
  DESCRIPTION                    = "The Compiler Company Core"
  LICENSE                        = "MPL"
  AST                            = NIL
  ID                             = 3733260365104583976
  VC                             = :HG
  RULES                          = #(#<SK-RULE :ID 05b0-0201-ab05-f17d> #<SK-RULE :ID 20c4-3005-bae0-0616>)
  DOCS                           = ((:ORG "readme") (:ORG "install") (:ORG "tests") (:ORG "todo"))
  COMPONENTS                     = NIL
  SCRIPTS                        = NIL
  SNIPPETS                       = NIL
  STASH                          = #P".stash"
  STORE                          = #<unbound slot>
  ABBREVS                        = NIL
  IMPORTS                        = ("lisp/skelfile" "rust/skelfile" "emacs/skelfile" "c/skelfile")
#+end_example
*** organ
#+begin_src shell :results output :exports both
./x run organ
#+end_src

#+RESULTS:
: #<ORGAN::ORG-DOCUMENT {1003D5C313}>
:   [standard-object]
: 
: Slots with :INSTANCE allocation:
:   META                           = #<ORGAN::ORG-META-SECTION {1003D5F1B3}>
:   TREE                           = #(#<ORGAN:ORG-HEADING {1003E40B53}>)

